// src/components/Header.js
import React from 'react';
import '../App.css'; // Make sure you style the header in its own CSS

const Header = () => {
  return (
    <header className="app-header">
      <h1>Task Manager App</h1>
    </header>
  );
};

export default Header;
